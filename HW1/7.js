const fileSize = 0.82;
var flashSize = prompt("Enter the size of the flash drive in GB: ");
var result = flashSize / fileSize;
alert(flashSize + " GB flash drive will fit " + result + " files of 820 MB each");